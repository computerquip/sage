{
  "name": "sage",
  "private": true,
  "version": "0.1.0",
  "description": "herdr + pi + gondolin sandboxed autonomous agent setup",
  "engines": {
    "node": ">=24.15.0"
  },
  "scripts": {
    "build:image": "sh image/build.sh"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  }
}
